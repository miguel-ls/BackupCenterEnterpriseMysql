RewriteEngine On

# Si el archivo o carpeta existe → servir normal
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Si no existe → enviar a index.html
RewriteRule ^ index.html [L]
